// Firebase web configuration is public, but Firestore and Storage rules must
// still protect the submitted proposals. Replace these empty values with the
// configuration shown in Firebase Console > Project settings > Your apps.
export const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};

export const appCheckSiteKey = "";
